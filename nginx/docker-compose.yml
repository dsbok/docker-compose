version: '3.8'

services:
  web:
    image: trafex/php-nginx:latest
    container_name: nginx-php
    restart: always
    ports:
      - "80:80"
    environment:
      - TZ=Asia/Kuala_Lumpur
    volumes:
      # Map your code to the image's default web root
      - /opt/stacks/nginx:/var/www/html
