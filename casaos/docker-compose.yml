services:
  casa:
    image: dockurr/casa
    container_name: casa
    ports:
      - 8083:8080
    volumes:
      - /opt/stacks/casaos:/DATA
      - /var/run/docker.sock:/var/run/docker.sock
    restart: always
    stop_grace_period: 1m
